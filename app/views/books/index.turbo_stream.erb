<%= turbo_stream.replace 'book-list' do %>
  <div id="book-list">
    <% @books.each do |book| %>
      <div class="book">
        <h3><%= book.title %></h3>
        <p><%= book.author %></p>
      </div>
    <% end %>
  </div>
<% end %>
